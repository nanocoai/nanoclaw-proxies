# Remove Iron Proxy gateway

Select another installed gateway before restarting NanoClaw. NanoClaw's uninstall flow removes the install-labelled Iron Proxy container and `data/gateways/iron-proxy` with this copy's other data.

Use the journal-derived skill removal to remove installed payload files. Do not delete a container whose `nanoclaw-install` label does not match this checkout.
