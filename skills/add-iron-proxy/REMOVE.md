# Remove Iron Proxy gateway

Select another installed gateway before restarting NanoClaw. Stopping each session removes its session-scoped Iron Proxy container. NanoClaw's uninstall flow removes this copy's gateway material with its other data.

Use the journal-derived skill removal to remove installed payload files.
